import { ModifiedcurrencyPipe } from './modifiedcurrency.pipe';

describe('ModifiedcurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new ModifiedcurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
